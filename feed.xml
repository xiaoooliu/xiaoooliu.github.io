<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://xiaoooliu.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://xiaoooliu.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-23T15:16:10+00:00</updated><id>https://xiaoooliu.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal academic website of Xiao Liu, a PhD student at Shanghai Jiao Tong University working on generative AI, reinforcement learning, and trustworthy AI systems. </subtitle><entry><title type="html">Reinforcement Learning: From Bellman Equations to World Models</title><link href="https://xiaoooliu.github.io/blog/2026/reinforcement-learning-from-bellman-to-world-models/" rel="alternate" type="text/html" title="Reinforcement Learning: From Bellman Equations to World Models"/><published>2026-04-20T02:00:00+00:00</published><updated>2026-04-20T02:00:00+00:00</updated><id>https://xiaoooliu.github.io/blog/2026/reinforcement-learning-from-bellman-to-world-models</id><content type="html" xml:base="https://xiaoooliu.github.io/blog/2026/reinforcement-learning-from-bellman-to-world-models/"><![CDATA[<p>Reinforcement learning (RL) studies how an agent should act through interaction so as to maximize long-term return. The field is mathematically elegant, historically deep, and still evolving rapidly. What I find most helpful is not to memorize RL as a flat list of algorithms, but to read it as a sequence of answers to a few recurring questions:</p> <ul> <li>How should we formalize delayed consequences?</li> <li>How can we estimate long-term value from partial experience?</li> <li>How do we improve a policy without destabilizing learning?</li> <li>When is it worth learning a model of the environment?</li> </ul> <p>This post follows that thread from Bellman equations and temporal-difference learning to policy gradients, deep RL, offline RL, and modern world models.</p> <h2 id="the-markov-decision-process-view">The Markov decision process view</h2> <p>The standard formalism of RL is the <strong>Markov Decision Process (MDP)</strong>, defined by states $s$, actions $a$, a transition kernel $P(s’ \mid s,a)$, a reward function $r(s,a)$, and a discount factor $\gamma \in [0,1)$. A policy $\pi(a \mid s)$ maps states to action distributions.</p> <p>For a given policy, the state-value function is</p> \[V^\pi(s) = \mathbb{E}_\pi\left[\sum_{t=0}^\infty \gamma^t r_t \,\middle|\, s_0=s\right],\] <p>and the action-value function is</p> \[Q^\pi(s,a) = \mathbb{E}_\pi\left[\sum_{t=0}^\infty \gamma^t r_t \,\middle|\, s_0=s, a_0=a\right].\] <p>These definitions are not merely bookkeeping devices. They encode the central RL difficulty: actions matter because they influence future state distributions, not just immediate rewards.</p> <p><strong>Read an MDP as a causal loop, not a static dataset.</strong> The agent chooses an action, the environment changes state, and the resulting reward is only one part of the evidence available for evaluating that decision.</p> <ul> <li><strong>State and transition.</strong> <em>What can happen next.</em> $P(s’ \mid s,a)$ captures the part of the world the agent must predict or reason through.</li> <li><strong>Policy.</strong> <em>How the agent intervenes.</em> $\pi(a \mid s)$ changes the future state distribution, which is why supervised prediction of immediate reward is not enough.</li> <li><strong>Return.</strong> <em>What is optimized.</em> Discounting makes the infinite-horizon objective well defined and expresses how much delayed outcomes should influence a current decision.</li> </ul> <h2 id="bellman-equations-as-the-backbone-of-rl">Bellman equations as the backbone of RL</h2> <p>The key recursion is the <strong>Bellman equation</strong>, introduced in <a href="https://press.princeton.edu/books/hardcover/9780691651873/dynamic-programming">Bellman (1957)</a>:</p> \[V^\pi(s) = \mathbb{E}_{a \sim \pi,\, s' \sim P} \left[r(s,a) + \gamma V^\pi(s')\right].\] <p>Similarly,</p> \[Q^\pi(s,a) = \mathbb{E}_{s' \sim P,\, a' \sim \pi} \left[r(s,a) + \gamma Q^\pi(s',a')\right].\] <p>For optimal control, the Bellman optimality equation becomes</p> \[Q^*(s,a) = \mathbb{E}_{s' \sim P} \left[r(s,a) + \gamma \max_{a'} Q^*(s',a')\right].\] <p>These recursions are the structural core of the field. Dynamic programming, temporal-difference learning, policy iteration, actor-critic methods, and many modern algorithms can all be understood as approximate ways of solving Bellman-style fixed-point equations under data and computation constraints.</p> <h2 id="dynamic-programming-and-the-model-based-ideal">Dynamic programming and the model-based ideal</h2> <p>If the environment dynamics $P$ and reward function are known, we can solve an MDP using <strong>dynamic programming</strong>. Classical value iteration repeatedly applies the Bellman optimality operator, while policy iteration alternates between policy evaluation and policy improvement.</p> <p>This setting is idealized but useful. It clarifies what makes RL hard in practice:</p> <ul> <li>the transition model is often unknown,</li> <li>the state space is too large for tabular enumeration,</li> <li>and exact backups become impossible under function approximation.</li> </ul> <p>Thus, most of RL is about making Bellman reasoning work approximately from sampled experience.</p> <h2 id="monte-carlo-and-temporal-difference-learning">Monte Carlo and temporal-difference learning</h2> <p>Two classical routes to policy evaluation are <strong>Monte Carlo</strong> estimation and <strong>temporal-difference (TD)</strong> learning.</p> <p>Monte Carlo methods wait until the end of a trajectory and regress value estimates toward full sampled returns. They are unbiased but can have high variance.</p> <p>TD methods bootstrap from current estimates instead. The simplest TD(0) update for state values is</p> \[V(s_t) \leftarrow V(s_t) + \alpha \left(r_t + \gamma V(s_{t+1}) - V(s_t)\right).\] <p>This target uses one-step lookahead and therefore has lower variance, but it introduces bias because the target depends on an estimate rather than a realized full return.</p> <p>The bias-variance tradeoff introduced here never disappears from RL. It reappears later in eligibility traces, actor-critic learning, advantage estimation, and modern large-scale policy optimization.</p> <h2 id="q-learning-and-off-policy-control">Q-learning and off-policy control</h2> <p><a href="https://link.springer.com/article/10.1007/BF00992698"><strong>Q-learning</strong></a> is one of the most important algorithms in RL because it provides a simple model-free route to optimal control. Its update is</p> \[Q(s_t,a_t) \leftarrow Q(s_t,a_t) + \alpha \Big( r_t + \gamma \max_{a'} Q(s_{t+1},a') - Q(s_t,a_t) \Big).\] <p>Several properties made Q-learning historically central.</p> <p>First, it is <strong>off-policy</strong>: the behavior policy used to collect data can differ from the greedy policy implied by the learned $Q$ function. Second, it uses bootstrapping, which makes learning sample-efficient in many discrete settings. Third, it avoids explicit model estimation.</p> <p>At the same time, Q-learning also exposes fundamental challenges:</p> <ul> <li>maximization can amplify estimation error,</li> <li>exploration is often difficult,</li> <li>and combining off-policy bootstrapping with nonlinear function approximation can become unstable.</li> </ul> <p>These challenges strongly shaped the next decades of RL research.</p> <h2 id="the-deadly-triad">The deadly triad</h2> <p>A classic warning in RL is the <strong>deadly triad</strong>:</p> <ul> <li>function approximation,</li> <li>bootstrapping,</li> <li>and off-policy learning.</li> </ul> <p>Individually, each ingredient is useful. Together, they can cause divergence or severe instability. Much of deep RL can be read as an engineering and algorithmic effort to tame the deadly triad with replay, target networks, regularization, trust regions, conservative objectives, and better data usage.</p> <p>Keeping this triad in mind is extremely helpful when reading new RL papers. Many methods differ in surface form, but they are often solving the same stability problem.</p> <h2 id="policy-gradients-optimize-the-policy-directly">Policy gradients: optimize the policy directly</h2> <p>Value-based methods are not always the most natural choice, especially in continuous action spaces. Policy-based methods instead parameterize the policy itself and optimize expected return directly.</p> <p>The <strong>policy gradient theorem</strong> of <a href="https://proceedings.neurips.cc/paper/1999/hash/464d828b85b0bed98e80ade0a5c43b0f-Abstract.html">Sutton et al. (1999)</a> states that</p> \[\nabla_\theta J(\theta) = \mathbb{E}_{\pi_\theta} \left[ \nabla_\theta \log \pi_\theta(a_t \mid s_t)\, Q^{\pi_\theta}(s_t,a_t) \right].\] <p>This result is important because it avoids differentiating the state distribution explicitly. In practice, one replaces $Q^{\pi_\theta}$ with sampled returns or lower-variance surrogates.</p> <p>The simplest algorithm, REINFORCE, uses full-return Monte Carlo estimates:</p> \[\nabla_\theta J(\theta) \approx \sum_t \nabla_\theta \log \pi_\theta(a_t \mid s_t)\, G_t.\] <p>This estimator is unbiased but often has high variance. That motivates baselines and critic networks.</p> <h2 id="advantage-functions-and-actor-critic-methods">Advantage functions and actor-critic methods</h2> <p>The <strong>advantage function</strong></p> \[A^\pi(s,a) = Q^\pi(s,a) - V^\pi(s)\] <p>measures how much better or worse an action is than the policy’s average behavior at a state. Replacing raw returns with advantages often reduces gradient variance substantially.</p> <p>This leads naturally to <strong>actor-critic</strong> methods:</p> <ul> <li>the <strong>actor</strong> updates the policy,</li> <li>the <strong>critic</strong> estimates value information used for the gradient.</li> </ul> <p>Conceptually, actor-critic is one of the most durable ideas in RL because it combines:</p> <ul> <li>the flexibility of direct policy parameterization,</li> <li>the sample reuse benefits of value estimation,</li> <li>and the possibility of continuous control.</li> </ul> <p>Many modern RL algorithms, from A2C/A3C to PPO and SAC, fit naturally into this actor-critic template.</p> <h2 id="deep-q-networks-and-the-birth-of-deep-rl">Deep Q-Networks and the birth of deep RL</h2> <p>The deep RL era was catalyzed by <a href="https://www.nature.com/articles/nature14236"><strong>DQN</strong></a>, which applied Q-learning to high-dimensional observations such as Atari frames. This was historically significant because it showed that convolutional neural networks could learn control policies directly from pixels with a single algorithmic template.</p> <p>Two stabilizing ideas were crucial:</p> <p><strong>Experience replay</strong> stores transitions in a replay buffer and samples them approximately independently for training. This breaks the strong temporal correlation of online trajectories, lets rare but useful transitions be reused, and makes neural-network optimization resemble ordinary supervised learning more closely. Its cost is that the data distribution becomes stale, so later methods often tune replay priorities, buffer age, and off-policy corrections carefully.</p> <p><strong>Target networks</strong> address a different instability: bootstrapping against a rapidly moving function approximator. DQN freezes a separate target network for several updates, turning a moving fixed-point problem into a sequence of better-behaved regression problems. Replay and targets are complementary; without either one, the combination of nonlinear approximation, bootstrapping, and off-policy data is markedly less stable.</p> <figure style="margin: 1.75rem auto; max-width: 760px;"> <img src="/assets/img/blog/dqn-seaquest-result.png" alt="Seaquest Atari frame used by DQN" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">An Atari Seaquest observation, representative of the raw visual input used in DQN. Reproduced from <a href="https://arxiv.org/abs/1312.5602">Mnih et al. (2013)</a>.</figcaption> </figure> <p>These ideas became part of the standard RL toolbox. Many later off-policy methods can be viewed as elaborations of replay-buffer learning with more careful targets and critics.</p> <h2 id="improvements-to-value-based-deep-rl">Improvements to value-based deep RL</h2> <p>After DQN, a long sequence of refinements followed:</p> <ul> <li><strong>Double DQN</strong> reduces overestimation bias by decoupling action selection from target evaluation.</li> <li><strong>Dueling networks</strong> separate state value and action advantage estimation.</li> <li><strong>Prioritized replay</strong> samples more informative transitions more often.</li> <li><strong>Distributional RL</strong> models the full return distribution instead of just its expectation.</li> </ul> <p>This period is instructive because it shows how deep RL progressed: not by one giant theoretical leap, but by repeatedly addressing specific failure modes in approximation, target bias, and data efficiency.</p> <h2 id="trust-regions-and-stable-policy-improvement">Trust regions and stable policy improvement</h2> <p>Policy gradient methods can also be unstable, especially when each update changes the policy too aggressively. <a href="https://proceedings.mlr.press/v37/schulman15.html"><strong>Trust Region Policy Optimization (TRPO)</strong></a> formalized the principle that policy updates should stay within a local trust region in distribution space.</p> <p>The basic idea is to maximize a surrogate objective while constraining the KL divergence between the new and old policies. This improved stability, but the optimization procedure is relatively cumbersome.</p> <p><a href="https://arxiv.org/abs/1707.06347"><strong>Proximal Policy Optimization (PPO)</strong></a> simplified the idea with the clipped objective</p> \[\mathcal{L}^{\mathrm{CLIP}}(\theta) = \mathbb{E} \left[ \min\left( r_t(\theta)\hat{A}_t,\, \operatorname{clip}(r_t(\theta),1-\epsilon,1+\epsilon)\hat{A}_t \right) \right],\] <p>where</p> \[r_t(\theta)=\frac{\pi_\theta(a_t \mid s_t)}{\pi_{\theta_{\mathrm{old}}}(a_t \mid s_t)}.\] <p>PPO became widely used because it hits a strong practical balance:</p> <ul> <li>simple to implement,</li> <li>relatively robust,</li> <li>broadly effective across control, robotics, and language-model fine-tuning settings.</li> </ul> <h2 id="generalized-advantage-estimation">Generalized advantage estimation</h2> <p>One of the practical tools that made actor-critic methods work well is <a href="https://arxiv.org/abs/1506.02438"><strong>Generalized Advantage Estimation (GAE)</strong></a>. It constructs advantage estimates by exponentially weighting multi-step TD residuals, trading off bias and variance smoothly.</p> <p>The reason GAE matters is that it turned what could have been a fragile estimation problem into a tunable engineering component. In large-scale policy optimization, the quality of the advantage estimator is often nearly as important as the optimizer itself.</p> <h2 id="continuous-control-ddpg-td3-and-sac">Continuous control: DDPG, TD3, and SAC</h2> <p>Many real control problems have continuous action spaces, where maximizing over actions inside a Bellman backup is not trivial. This motivated specialized actor-critic methods.</p> <p><strong>DDPG</strong> combines deterministic policy gradients with replay-buffer training and target networks. It was influential because it showed that deep off-policy actor-critic learning could handle continuous actions, but it is often brittle: actor errors can exploit optimistic critic errors and quickly destabilize learning.</p> <p><strong>TD3</strong> improves this failure mode with clipped double critics, delayed policy updates, and target-policy smoothing. The common theme is conservative value estimation: when the critic is uncertain, avoiding systematic overestimation is often more important than pushing the actor toward the nominally best action.</p> <p><strong>Soft Actor-Critic (SAC)</strong> introduced a maximum-entropy objective</p> \[J(\pi) = \sum_t \mathbb{E}_{(s_t,a_t)\sim \pi} \left[ r(s_t,a_t) + \alpha \mathcal{H}(\pi(\cdot \mid s_t)) \right].\] <p>The entropy term encourages stochastic exploration and often produces more stable training. SAC is one of the most practically important RL algorithms because it combines strong sample efficiency with impressive robustness in continuous control.</p> <h2 id="model-based-reinforcement-learning">Model-based reinforcement learning</h2> <p>Model-free RL avoids explicit environment modeling, but it can be expensive in sample complexity. This motivates <strong>model-based RL</strong>, where the agent learns or uses a dynamics model to support planning, imagination, or better policy improvement.</p> <p>There are several ways a model can be used:</p> <ul> <li>simulate rollouts for planning,</li> <li>improve value estimation,</li> <li>generate synthetic training data,</li> <li>or learn compact latent dynamics for control in representation space.</li> </ul> <p>Historically, model-based RL has swung between optimism and disappointment. Simple learned models often accumulate prediction error over long horizons, causing compounding bias in planning. Recent world-model methods became much more successful by modeling latent structure rather than raw observations directly.</p> <h2 id="world-models-and-latent-imagination">World models and latent imagination</h2> <p>Modern <strong>world models</strong> typically learn:</p> <ul> <li>a representation model mapping observations to latent states,</li> <li>a transition model in latent space,</li> <li>and reward or value heads on top of those latent dynamics.</li> </ul> <p>This makes imagined rollouts cheaper and often more stable than pixel-space prediction. The resulting framework is particularly appealing when real interaction is expensive, as in robotics, autonomous experimentation, or safety-constrained decision making.</p> <p>Methods in the <strong>Dreamer</strong> family are especially important milestones. They show that latent imagination can be used not just for toy planning, but as a scalable control strategy across diverse domains. <a href="https://arxiv.org/abs/2301.04104"><strong>DreamerV3</strong></a> is notable for reducing per-domain retuning and improving robustness, suggesting that world-model RL is becoming more generally usable.</p> <figure style="margin: 1.75rem auto; max-width: 760px;"> <img src="/assets/img/blog/dreamerv3-world-model-learning.png" alt="DreamerV3 world model learning architecture" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">DreamerV3 world-model learning: observations are encoded into discrete latents and predicted through recurrent dynamics. Reproduced from <a href="https://arxiv.org/abs/2301.04104">Hafner et al. (2023)</a>.</figcaption> </figure> <figure style="margin: 1.75rem auto; max-width: 900px;"> <img src="/assets/img/blog/dreamerv3-cross-domain-results.png" alt="DreamerV3 results across RL benchmark domains" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">DreamerV3 results across diverse domains, showing one configuration used across benchmark families. Reproduced from <a href="https://arxiv.org/abs/2301.04104">Hafner et al. (2023)</a>.</figcaption> </figure> <h2 id="offline-rl">Offline RL</h2> <p>Another major modern direction is <strong>offline RL</strong>, where the policy must be learned from a fixed dataset without further interaction. This setting is attractive when exploration is unsafe, expensive, or impossible.</p> <p>Offline RL introduces a distinctive problem: the learned policy may choose actions not well supported by the dataset, leading to severe extrapolation error in value estimation. This motivates conservative critics, behavior regularization, and sequence-model-based approaches.</p> <p>The offline setting is highly relevant for real applications because many domains already have logged historical data, but do not permit unconstrained online trial and error.</p> <h2 id="rl-as-sequence-modeling">RL as sequence modeling</h2> <p>Recent work has also reframed RL as a sequence modeling problem. <a href="https://arxiv.org/abs/2106.01345"><strong>Decision Transformer</strong></a> is a clean example: instead of explicitly learning Bellman backups, it models trajectories autoregressively conditioned on a target return.</p> <p>This is conceptually striking because it shows that some RL problems can be attacked with tools from language modeling and supervised sequence prediction. It does not replace all of RL, but it broadens the design space. In data-rich settings, especially offline ones, the boundary between control and sequence modeling becomes much less rigid.</p> <h2 id="exploration-credit-assignment-and-long-horizons">Exploration, credit assignment, and long horizons</h2> <p>Despite decades of progress, several core difficulties remain central.</p> <p><strong>Exploration</strong> is difficult because reward signals can be sparse, delayed, or deceptive. Count-based bonuses, intrinsic motivation, entropy regularization, and curiosity objectives all add an incentive for visiting informative states, but they must be balanced against safety and task reward; novelty alone is not useful if it repeatedly drives the agent into irrelevant or unsafe regions.</p> <p><strong>Credit assignment</strong> becomes hard when a reward arrives long after the decision that enabled it. Eligibility traces, advantage estimation, temporal abstractions, and learned models all supply shortcuts for moving learning signal backward through time, yet long-horizon credit remains an open problem whenever relevant events are rare or partially observed.</p> <p><strong>Distribution shift</strong> links off-policy, offline, and model-based RL. A critic may evaluate actions that the dataset never supported, or a planner may optimize trajectories a learned model predicts poorly. Conservative objectives, uncertainty estimates, and explicit behavior regularization are therefore not optional add-ons; they are defenses against acting outside the evidence available to the learner.</p> <h2 id="a-practical-taxonomy">A practical taxonomy</h2> <p>I find it useful to organize RL methods by what they estimate most directly.</p> <p><strong>Value-based methods</strong> learn $V$ or $Q$ and derive a policy indirectly, typically by maximizing action value. They are natural for discrete control and can reuse data efficiently, but bootstrapping, off-policy data, and function approximation can interact unstably.</p> <p><strong>Policy-based methods</strong> parameterize the policy directly and optimize expected return. They are well suited to continuous or stochastic control, but variance reduction, trust regions, and exploration are central because policy gradients are estimated from noisy trajectories.</p> <p><strong>Actor-critic methods</strong> combine direct policy optimization with a learned value estimator. This is the dominant practical template because the critic reduces variance while the actor can represent rich continuous-action policies; most differences among modern algorithms concern how conservatively the critic is trained and how far the actor may move per update.</p> <p><strong>Model-based methods</strong> learn or exploit dynamics to improve planning and sample efficiency. They are especially attractive for data-limited, high-cost environments, but they must control compounding model error by planning in suitable latent spaces, keeping imagined rollouts short, or estimating uncertainty.</p> <h2 id="why-rl-still-matters">Why RL still matters</h2> <p>Reinforcement learning remains important because it addresses a setting that ordinary supervised learning does not fully capture: <strong>decision making under delayed consequences and interaction constraints</strong>.</p> <p>This is especially relevant for trustworthy AI and scientific discovery. Many systems in those domains must:</p> <ul> <li>optimize long-horizon objectives,</li> <li>balance exploration against risk,</li> <li>satisfy constraints under uncertainty,</li> <li>and learn from interaction rather than static labels alone.</li> </ul> <p>Those are fundamentally RL-shaped problems, even when the eventual solution uses ideas from sequence modeling, control theory, or world models.</p> <h2 id="outlook">Outlook</h2> <p>The field is moving toward greater integration. Deep RL now borrows heavily from supervised learning, generative modeling, and large-scale sequence modeling. World models and offline RL are making the data interface more realistic. Policy optimization remains essential, but increasingly it is embedded inside richer systems for representation learning, planning, and uncertainty management.</p> <p>If there is one durable lesson across the history of RL, it is that the Bellman view never really disappears. Even when we use transformers, latent dynamics, or conservative offline objectives, we are still grappling with the same central issue Bellman formalized long ago: how should present decisions be evaluated through their effect on the future?</p> <h2 id="references">References</h2> <ol> <li>Richard Bellman. <em>Dynamic Programming</em>. Princeton University Press, 1957. https://press.princeton.edu/books/hardcover/9780691651873/dynamic-programming</li> <li>Richard S. Sutton and Andrew G. Barto. <em>Reinforcement Learning: An Introduction</em>. Second Edition, 2018. http://incompleteideas.net/book/the-book-2nd.html</li> <li>Christopher J. C. H. Watkins and Peter Dayan. <em>Q-learning</em>. Machine Learning, 1992. https://link.springer.com/article/10.1007/BF00992698</li> <li>Richard S. Sutton, David McAllester, Satinder Singh, and Yishay Mansour. <em>Policy Gradient Methods for Reinforcement Learning with Function Approximation</em>. NeurIPS 1999. https://proceedings.neurips.cc/paper/1999/hash/464d828b85b0bed98e80ade0a5c43b0f-Abstract.html</li> <li>Volodymyr Mnih et al. <em>Human-level Control through Deep Reinforcement Learning</em>. Nature, 2015. https://www.nature.com/articles/nature14236</li> <li>John Schulman, Sergey Levine, Philipp Moritz, Michael Jordan, and Pieter Abbeel. <em>Trust Region Policy Optimization</em>. ICML 2015. https://proceedings.mlr.press/v37/schulman15.html</li> <li>Timothy P. Lillicrap et al. <em>Continuous Control with Deep Reinforcement Learning</em>. 2015. https://arxiv.org/abs/1509.02971</li> <li>John Schulman, Filip Wolski, Prafulla Dhariwal, Alec Radford, and Oleg Klimov. <em>Proximal Policy Optimization Algorithms</em>. 2017. https://arxiv.org/abs/1707.06347</li> <li>Tuomas Haarnoja, Aurick Zhou, Pieter Abbeel, and Sergey Levine. <em>Soft Actor-Critic: Off-Policy Maximum Entropy Deep Reinforcement Learning with a Stochastic Actor</em>. ICML 2018. https://proceedings.mlr.press/v80/haarnoja18b.html</li> <li>Hado van Hasselt, Arthur Guez, and David Silver. <em>Deep Reinforcement Learning with Double Q-learning</em>. AAAI 2016. https://arxiv.org/abs/1509.06461</li> <li>Lili Chen et al. <em>Decision Transformer: Reinforcement Learning via Sequence Modeling</em>. NeurIPS 2021. https://arxiv.org/abs/2106.01345</li> <li>Danijar Hafner, Jurgis Pasukonis, Jimmy Ba, and Timothy Lillicrap. <em>Mastering Diverse Domains through World Models</em>. 2023. https://arxiv.org/abs/2301.04104</li> </ol>]]></content><author><name></name></author><category term="research"/><category term="reinforcement-learning"/><category term="deep-rl"/><category term="world-models"/><summary type="html"><![CDATA[A technical overview of reinforcement learning from dynamic programming and Q-learning to deep RL, policy optimization, and modern world models.]]></summary></entry><entry><title type="html">Flow-based Models: From Coupling Layers to Flow Matching</title><link href="https://xiaoooliu.github.io/blog/2025/flow-based-models-from-coupling-layers-to-flow-matching/" rel="alternate" type="text/html" title="Flow-based Models: From Coupling Layers to Flow Matching"/><published>2025-12-07T02:00:00+00:00</published><updated>2025-12-07T02:00:00+00:00</updated><id>https://xiaoooliu.github.io/blog/2025/flow-based-models-from-coupling-layers-to-flow-matching</id><content type="html" xml:base="https://xiaoooliu.github.io/blog/2025/flow-based-models-from-coupling-layers-to-flow-matching/"><![CDATA[<p>Flow-based generative models occupy a special place in modern machine learning. They offer an unusually clean mathematical story: if we can learn an invertible transformation from a simple base distribution to the data distribution, then we obtain exact likelihood, exact latent-variable inference, and direct ancestral sampling in one framework. This is a very different philosophy from adversarial generation or diffusion-based denoising. Instead of learning to classify realism or reverse a corruption process, flows learn transport maps whose density transformation can be computed explicitly.</p> <p>The classical form of this idea is the <strong>normalizing flow</strong>, but the area has expanded far beyond stacks of coupling layers. Continuous normalizing flows, invertible residual networks, stochastic interpolants, flow matching, and rectified flow have all pushed the field from “tractable Jacobians” toward a broader language of probability transport. This post follows that arc, from early discrete invertible architectures to more recent path-based formulations.</p> <h2 id="change-of-variables-as-the-foundation">Change of variables as the foundation</h2> <p>Let $z \sim p_Z(z)$ be a simple latent variable, often standard Gaussian, and let $x = f(z)$ for an invertible differentiable map $f$. Then the density of $x$ is given by the change-of-variables formula</p> \[p_X(x) = p_Z(f^{-1}(x)) \left|\det J_{f^{-1}}(x)\right|,\] <p>or equivalently</p> \[\log p_X(x) = \log p_Z(f^{-1}(x)) + \log \left|\det J_{f^{-1}}(x)\right|.\] <p>This equation is the core of flow-based modeling. It immediately provides three capabilities:</p> <ul> <li><strong>Sampling.</strong> <em>Prior to data.</em> Draw $z \sim p_Z$ and push it through $f$; this direction is attractive when the forward map is cheap.</li> <li><strong>Likelihood evaluation.</strong> <em>Data to prior.</em> Invert $x$ and add the Jacobian correction. This is the defining advantage over generators that only support sampling.</li> <li><strong>Latent inference.</strong> <em>An exact representation.</em> Apply $f^{-1}$ to map an observation into a latent coordinate system rather than relying on a separately trained encoder.</li> </ul> <p>The architectural problem is therefore very precise: we need invertible maps that are expressive enough for complex data distributions while keeping the log-determinant of the Jacobian computationally tractable.</p> <h2 id="why-invertibility-is-both-a-strength-and-a-constraint">Why invertibility is both a strength and a constraint</h2> <p>Invertibility gives flows mathematical transparency, but it also imposes a strong inductive bias. A generic deep network is not invertible, and even when a transformation is invertible in principle, computing the determinant of its Jacobian may be prohibitively expensive in high dimensions.</p> <p>This created the central tension in early flow research:</p> <ul> <li>If we enforce too much structure, optimization and likelihood are easy but expressivity is weak.</li> <li>If we allow very flexible transformations, likelihood computation becomes intractable.</li> </ul> <p>Much of the history of flows can be read as a sequence of compromises around this tension.</p> <h2 id="nice-coupling-layers-and-triangular-jacobians">NICE: coupling layers and triangular Jacobians</h2> <p>The breakthrough that made normalizing flows practical was the <strong>coupling layer</strong> design in <a href="https://arxiv.org/abs/1410.8516"><strong>NICE</strong></a>. Split the input into two parts, $x = (x_a, x_b)$, and transform only one part conditioned on the other:</p> \[y_a = x_a, \qquad y_b = x_b + m(x_a).\] <p>This simple form has major advantages.</p> <p>First, the inverse is trivial:</p> \[x_a = y_a, \qquad x_b = y_b - m(y_a).\] <p>Second, the Jacobian is triangular, so its determinant is easy to compute. For additive coupling, the determinant is exactly one, making the log-determinant term disappear.</p> <p>NICE established an enduring design principle: if each layer only transforms part of the state while leaving the rest unchanged, then exact invertibility becomes manageable. However, additive coupling also limits local rescaling and can therefore be less expressive than one would like for complex image densities.</p> <h2 id="realnvp-affine-coupling-and-multi-scale-design">RealNVP: affine coupling and multi-scale design</h2> <p><a href="https://arxiv.org/abs/1605.08803"><strong>RealNVP</strong></a> extended NICE by introducing <strong>affine coupling</strong>:</p> \[y_a = x_a, \qquad y_b = x_b \odot \exp(s(x_a)) + t(x_a).\] <p>Now the Jacobian is still triangular, but no longer volume-preserving. Its log-determinant becomes</p> \[\log |\det J| = \sum_i s_i(x_a).\] <p>This was an important step because the model can now scale as well as shift features. RealNVP also introduced a multi-scale architecture that progressively factors out subsets of variables, improving computational efficiency and stabilizing training on images.</p> <p>RealNVP became one of the first flow models that felt like a serious large-scale generative system rather than a conceptual proof of principle. It demonstrated that exact likelihood and usable image synthesis could coexist.</p> <figure style="margin: 1.75rem auto; max-width: 760px;"> <img src="/assets/img/blog/realnvp-attribute-transfer.png" alt="RealNVP attribute transfer examples" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">Latent-space attribute transfer with RealNVP, illustrating the semantic structure available through an invertible representation. Reproduced from <a href="https://arxiv.org/abs/1605.08803">Dinh, Sohl-Dickstein, and Bengio (2017)</a>.</figcaption> </figure> <h2 id="autoregressive-flows-and-directional-tradeoffs">Autoregressive flows and directional tradeoffs</h2> <p>Another important branch of early development is the autoregressive family. Here, each output dimension depends on previous dimensions in an autoregressive order, which again yields a triangular Jacobian.</p> <p>Two canonical examples are:</p> <ul> <li><a href="https://arxiv.org/abs/1705.07057"><strong>MAF</strong></a>: efficient for density estimation, slower for sampling.</li> <li><a href="https://arxiv.org/abs/1606.04934"><strong>IAF</strong></a>: efficient for sampling, slower for exact density evaluation.</li> </ul> <p>This asymmetry is conceptually useful. It shows that “normalizing flow” is not one architecture, but a design space in which we may prioritize different computational directions.</p> <p>If our main goal is maximum-likelihood density estimation, MAF is attractive. If our goal is fast ancestral generation or variational inference, IAF may be preferable. The broader lesson is that tractability is directional: invertibility alone does not guarantee that all operations are equally cheap.</p> <h2 id="glow-and-the-refinement-of-image-flows">Glow and the refinement of image flows</h2> <p><a href="https://arxiv.org/abs/1807.03039"><strong>Glow</strong></a> refined the coupling-layer recipe with three influential ingredients:</p> <ul> <li><strong>ActNorm</strong>, a learnable affine normalization initialized from data,</li> <li><strong>invertible $1\times1$ convolutions</strong>, replacing fixed channel permutations,</li> <li>and a carefully engineered hierarchical architecture.</li> </ul> <p>The invertible convolution is especially elegant. A fixed permutation mixes channels only in a very restricted way, whereas a learnable linear transform can rotate feature channels before the next coupling block, greatly improving expressivity while preserving tractable determinants.</p> <p>Glow mattered historically because it showed that carefully engineered flow architectures could scale to visually impressive image generation, not merely likelihood benchmarks.</p> <figure style="margin: 1.75rem auto; max-width: 900px;"> <img src="/assets/img/blog/glow-latent-interpolation.png" alt="Glow latent interpolation" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">Smooth latent interpolation in Glow. Reproduced from <a href="https://arxiv.org/abs/1807.03039">Kingma and Dhariwal (2018)</a>.</figcaption> </figure> <h2 id="likelihood-quality-versus-sample-quality">Likelihood quality versus sample quality</h2> <p>One recurring theme in generative modeling is that strong likelihood does not automatically imply perceptually superior samples. Flows made this tension very visible. On one hand, they optimize exact log-likelihood rather than a variational bound or adversarial proxy. On the other hand, the invertibility constraint can make it hard to concentrate modeling capacity on perceptually salient directions in the way that GANs or later diffusion models often do.</p> <p>This observation is not an argument against flows. Rather, it clarifies what they optimize well:</p> <ul> <li>precise density modeling,</li> <li>invertible latent semantics,</li> <li>principled uncertainty under exact change of variables.</li> </ul> <p>When those properties matter, flows remain unusually attractive.</p> <h2 id="continuous-normalizing-flows">Continuous normalizing flows</h2> <p>The next conceptual shift was to replace a finite stack of invertible layers with a continuous-time trajectory. Instead of writing</p> \[x = f_K \circ f_{K-1} \circ \cdots \circ f_1(z),\] <p>we define a time-dependent state $z_t$ satisfying the ODE</p> \[\frac{d z_t}{dt} = v_\theta(z_t,t).\] <p>This leads to <strong>Continuous Normalizing Flows (CNFs)</strong>, built on the continuous-depth view of <a href="https://arxiv.org/abs/1806.07366">Neural ODEs</a>. The density evolves according to the instantaneous change-of-variables formula</p> \[\frac{d}{dt}\log p(z_t) = -\operatorname{tr}\left(\frac{\partial v_\theta}{\partial z_t}\right).\] <p>Integrating this from initial to final time yields the exact log-density correction. This perspective is powerful because it turns depth into time and lets us use adaptive ODE solvers. It also places generative modeling close to dynamical systems and optimal transport.</p> <h2 id="neural-odes-and-ffjord">Neural ODEs and FFJORD</h2> <p><a href="https://arxiv.org/abs/1806.07366">Neural ODEs</a> provided the general continuous-depth framework, while <a href="https://arxiv.org/abs/1810.01367"><strong>FFJORD</strong></a> showed how to make CNFs practical for high-dimensional density estimation using stochastic trace estimation.</p> <p>The essential computational challenge in CNFs is the trace term</p> \[\operatorname{tr}\left(\frac{\partial v_\theta}{\partial z_t}\right),\] <p>which is expensive to compute directly. Hutchinson-style estimators reduce this cost by replacing exact traces with unbiased stochastic estimates. This makes large-scale training feasible, though not always cheap.</p> <p>CNFs offer elegant geometry and adaptive trajectories, but they trade architectural constraints for numerical ones. The model is now simple to describe functionally, yet training and sampling depend on ODE integration accuracy, stiffness, and solver cost.</p> <h2 id="residual-flows-and-lipschitz-constrained-invertibility">Residual flows and Lipschitz-constrained invertibility</h2> <p>Another direction asked whether standard residual networks could be made invertible with mild constraints. Consider a residual block</p> \[f(x) = x + g(x).\] <p>If $g$ is sufficiently contractive, then $f$ is invertible. This gave rise to <a href="https://arxiv.org/abs/1811.00995"><strong>invertible residual networks</strong></a> and residual flows, where spectral or Lipschitz constraints ensure bijectivity.</p> <p>This perspective is valuable because it narrows the conceptual gap between conventional deep learning and invertible generative modeling. Instead of building special-purpose coupling architectures, one modifies familiar residual blocks to preserve invertibility. The resulting models connect flow design to stability theory, fixed-point inversion, and operator norms.</p> <h2 id="the-transport-viewpoint-becomes-central">The transport viewpoint becomes central</h2> <p>Up to this point, much of flow research was driven by exact likelihood and tractable Jacobians. More recent work shifts the emphasis. The main object is no longer always an explicitly invertible block architecture; it is often a <strong>probability path</strong> from a simple source distribution to the target data distribution.</p> <p>This shift matters because once we think in terms of paths rather than discrete layer stacks, we can connect flows to:</p> <ul> <li>optimal transport,</li> <li>Schrödinger bridge methods,</li> <li>diffusion probability paths,</li> <li>and deterministic samplers for score models.</li> </ul> <p>In other words, the modern frontier is increasingly about <strong>how to choose and learn transport trajectories</strong>, not just how to multiply Jacobian determinants efficiently.</p> <h2 id="flow-matching">Flow matching</h2> <p><a href="https://arxiv.org/abs/2210.02747"><strong>Flow Matching</strong></a> is one of the clearest expressions of this modern view. Rather than learning densities through repeated Jacobian corrections, it directly learns a time-dependent vector field whose trajectories realize a desired probability path.</p> <p>Suppose we define a conditional interpolation between a source sample $x_0$ and a target sample $x_1$, producing intermediate states $x_t$. Then we can derive a target velocity field $u_t(x_t \mid x_0, x_1)$ and train a model by regression:</p> \[\mathcal{L}_{\mathrm{FM}} = \mathbb{E}_{t,x_0,x_1,x_t} \left[ \left\|v_\theta(x_t,t) - u_t(x_t \mid x_0,x_1)\right\|_2^2 \right].\] <p>This looks deceptively straightforward, but it is conceptually important. Generation becomes the task of learning a transport ODE that matches a prescribed probability path, often without the need for stochastic reverse diffusion or likelihood-oriented layer design.</p> <p>Flow matching is attractive because:</p> <ul> <li>it supports deterministic generation,</li> <li>it is compatible with a wide family of probability paths,</li> <li>and it often leads to simpler and faster samplers than diffusion-style ancestral chains.</li> </ul> <figure style="margin: 1.75rem auto; max-width: 760px;"> <img src="/assets/img/blog/flow-matching-ot-path.png" alt="Flow Matching optimal-transport probability path" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">An optimal-transport probability path from a Gaussian source to a checkerboard target. Reproduced from <a href="https://arxiv.org/abs/2210.02747">Lipman et al. (2023)</a>.</figcaption> </figure> <h2 id="relation-to-diffusion-and-probability-paths">Relation to diffusion and probability paths</h2> <p>One reason flow matching has become influential is that it can reuse probability paths inspired by diffusion. If a path gradually transforms a simple prior into data, we may train a vector field to follow that path directly instead of learning a stochastic reverse denoiser.</p> <p>This creates a useful bridge between flows and diffusion:</p> <ul> <li>diffusion emphasizes reverse stochastic denoising,</li> <li>flow matching emphasizes deterministic velocity learning along probability paths.</li> </ul> <p>These are not disjoint camps. In many modern papers, they are two parameterizations of a deeper transport problem.</p> <h2 id="rectified-flow-and-straight-trajectories">Rectified flow and straight trajectories</h2> <p><a href="https://arxiv.org/abs/2209.03003"><strong>Rectified Flow</strong></a> pushes the transport viewpoint further by emphasizing <strong>straightness</strong> of trajectories. The intuition is simple: if sample paths are unnecessarily curved, then the ODE solver needs more steps and numerical error accumulates more easily. By learning straighter transport fields, one can often generate high-quality samples with fewer evaluations.</p> <p>This is appealing for both conceptual and practical reasons.</p> <p>Conceptually, it aligns generative modeling with efficient transport geometry. Practically, it speaks directly to the question that matters most in deployment: how many model evaluations are needed to produce a strong sample?</p> <p>Rectified flow also influenced the broader idea that probability-path design is a first-class object. We do not merely fit a vector field to whatever path is convenient; we choose paths that are easier to learn and easier to integrate.</p> <h2 id="stochastic-interpolants-and-unification">Stochastic interpolants and unification</h2> <p>Related work on <a href="https://arxiv.org/abs/2303.08797"><strong>stochastic interpolants</strong></a> offers an even broader framework for connecting diffusions, ODE flows, and other transport-based generators. The central message is that many apparently different generative models can be interpreted as learning dynamics over interpolating distributions between source and target.</p> <p>This is one of the most interesting conceptual developments in recent years. It suggests that the real unifying object is not “flow” or “diffusion” as a label, but rather:</p> <ul> <li>a family of intermediate distributions,</li> <li>a rule for moving probability mass across that family,</li> <li>and an objective that matches the corresponding local transport statistics.</li> </ul> <h2 id="what-changed-from-early-flows-to-recent-flows">What changed from early flows to recent flows</h2> <p>The historical development can be summarized as a sequence of broadening viewpoints.</p> <p><strong>From invertible layers to dynamical systems.</strong> Early flows focused on finite-dimensional blocks whose Jacobians were deliberately tractable. CNFs retain the change-of-variables principle but move the design problem to a time-dependent vector field, trading discrete architectural constraints for ODE integration and trace estimation.</p> <p><strong>From exact Jacobians to learned velocities.</strong> Classical flows place the log-determinant at center stage because likelihood is the primary objective. Flow matching instead regresses a velocity field along a chosen probability path; this often removes the need for special coupling structure, but it makes the chosen path and numerical solver central modeling choices.</p> <p><strong>From density estimation to transport design.</strong> The modern question is not only how to represent a density, but how to move probability mass from prior to data along paths that are stable, efficient, and expressive. This explains why optimal transport, diffusion paths, rectification, and stochastic interpolants now appear in the same technical conversation.</p> <h2 id="practical-tradeoffs">Practical tradeoffs</h2> <p>For implementation and paper reading, I find the following tradeoffs most useful.</p> <p><strong>Exact likelihood versus computational simplicity.</strong> Coupling-based flows provide exact likelihood in a transparent way, but they demand architectural constraints such as triangular Jacobians. Path-based methods can be simpler to train for generation, although they may emphasize transport quality rather than likelihood evaluation; the right choice depends on whether density estimation or sampling is the primary deliverable.</p> <p><strong>Invertibility versus flexibility.</strong> Hard invertibility guarantees are mathematically elegant but restrict layer design and memory tradeoffs. Continuous-time and path-based approaches relax this pressure by shifting complexity into vector fields and numerical solvers, which in turn introduces sensitivity to stiffness, tolerances, and solver choice.</p> <p><strong>Solver cost versus path quality.</strong> Once generation is expressed as ODE transport, numerical integration becomes central. A well-designed, nearly straight path can reduce the number of function evaluations dramatically, whereas a poor path can erase the apparent advantage of an otherwise expressive network.</p> <h2 id="why-flow-based-ideas-still-matter">Why flow-based ideas still matter</h2> <p>Even though diffusion models currently dominate many generative benchmarks, flow-based ideas remain deeply relevant.</p> <p>First, they provide one of the clearest mathematical languages for <strong>invertible transport</strong> and exact density transformation. Second, they connect generative modeling to ODEs, optimal transport, and continuous dynamics in a way that is often more explicit than denoising-based formulations. Third, recent flow-matching-style methods are actively influencing the design of fast generative samplers and hybrid diffusion-flow systems.</p> <p>For scientific machine learning, this matters a great deal. Many problems are really transport problems in disguise: moving between distributions over structures, trajectories, fields, or designs while preserving geometric constraints. The flow perspective is often the most natural lens for those tasks.</p> <h2 id="outlook">Outlook</h2> <p>The most interesting recent trend is that the boundary between flows and other generative paradigms has become softer. What used to be a narrowly defined architecture family is now part of a broader theory of probability paths and transport fields. This is healthy for the field. It means we can keep the conceptual precision of flow-based modeling while borrowing the strongest ideas from diffusion, optimal transport, and modern numerical methods.</p> <p>If one wants a compact takeaway, it is this: the original flow literature taught us how to make density transformation exact, while the newer literature teaches us how to make probability transport efficient. Both ideas are still central.</p> <h2 id="references">References</h2> <ol> <li>Laurent Dinh, David Krueger, and Yoshua Bengio. <em>NICE: Non-linear Independent Components Estimation</em>. 2014. https://arxiv.org/abs/1410.8516</li> <li>Laurent Dinh, Jascha Sohl-Dickstein, and Samy Bengio. <em>Density Estimation using Real NVP</em>. ICLR 2017. https://arxiv.org/abs/1605.08803</li> <li>Diederik P. Kingma, Tim Salimans, Rafal Jozefowicz, Xi Chen, Ilya Sutskever, and Max Welling. <em>Improving Variational Inference with Inverse Autoregressive Flow</em>. NeurIPS 2016. https://arxiv.org/abs/1606.04934</li> <li>George Papamakarios, Theo Pavlakou, and Iain Murray. <em>Masked Autoregressive Flow for Density Estimation</em>. NeurIPS 2017. https://arxiv.org/abs/1705.07057</li> <li>Diederik P. Kingma and Prafulla Dhariwal. <em>Glow: Generative Flow with Invertible 1x1 Convolutions</em>. NeurIPS 2018. https://arxiv.org/abs/1807.03039</li> <li>Ricky T. Q. Chen, Yulia Rubanova, Jesse Bettencourt, and David Duvenaud. <em>Neural Ordinary Differential Equations</em>. NeurIPS 2018. https://arxiv.org/abs/1806.07366</li> <li>Will Grathwohl, Ricky T. Q. Chen, Jesse Bettencourt, Ilya Sutskever, and David Duvenaud. <em>FFJORD: Free-form Continuous Dynamics for Scalable Reversible Generative Models</em>. ICLR 2019. https://arxiv.org/abs/1810.01367</li> <li>Jens Behrmann, Will Grathwohl, Ricky T. Q. Chen, David Duvenaud, and Jörn-Henrik Jacobsen. <em>Invertible Residual Networks</em>. ICML 2019. https://arxiv.org/abs/1811.00995</li> <li>Yaron Lipman, Ricky T. Q. Chen, Heli Ben-Hamu, Maximilian Nickel, and Matt Le. <em>Flow Matching for Generative Modeling</em>. ICLR 2023. https://arxiv.org/abs/2210.02747</li> <li>Xingchao Liu, Chengyue Gong, and Qiang Liu. <em>Flow Straight and Fast: Learning to Generate and Transfer Data with Rectified Flow</em>. ICLR 2023. https://arxiv.org/abs/2209.03003</li> <li>Michael S. Albergo, Nicholas M. Boffi, and Eric Vanden-Eijnden. <em>Stochastic Interpolants: A Unifying Framework for Flows and Diffusions</em>. 2023. https://arxiv.org/abs/2303.08797</li> </ol>]]></content><author><name></name></author><category term="research"/><category term="flow-based-models"/><category term="normalizing-flows"/><category term="generative-ai"/><summary type="html"><![CDATA[A technical review of flow-based generative models, from NICE and RealNVP to continuous flows, flow matching, and rectified flow.]]></summary></entry><entry><title type="html">Diffusion Models: From Langevin Dynamics to Discrete Diffusion</title><link href="https://xiaoooliu.github.io/blog/2025/diffusion-models-from-langevin-to-discrete-diffusion/" rel="alternate" type="text/html" title="Diffusion Models: From Langevin Dynamics to Discrete Diffusion"/><published>2025-06-12T02:00:00+00:00</published><updated>2025-06-12T02:00:00+00:00</updated><id>https://xiaoooliu.github.io/blog/2025/diffusion-models-from-langevin-to-discrete-diffusion</id><content type="html" xml:base="https://xiaoooliu.github.io/blog/2025/diffusion-models-from-langevin-to-discrete-diffusion/"><![CDATA[<p>Diffusion models have become one of the most influential paradigms in modern generative modeling. Their rise is not just an empirical story about image synthesis quality; it is also a mathematical story about how denoising, score estimation, stochastic dynamics, and probability transport fit together. What makes the area especially rich is that several lines of work that once looked separate now form a reasonably coherent picture: Langevin sampling, score matching, denoising diffusion probabilistic models, non-Markovian samplers, guidance methods, and more recent discrete-state variants.</p> <p>This post gives a technical overview of that picture. I focus on the parts that are most useful for reading papers and implementing models in practice: the score-based starting point, the DDPM formulation, DDIM acceleration, guided diffusion, and discrete diffusion models.</p> <h2 id="why-diffusion-models-are-conceptually-different">Why diffusion models are conceptually different</h2> <p>Classical likelihood-based generative models usually start by parameterizing a density directly or by constructing an invertible map from a simple latent distribution to data. Diffusion models take a different route. Instead of learning the data distribution in one shot, they define a sequence of slightly corrupted distributions and train a model to invert that corruption progressively.</p> <p>This viewpoint has three important consequences.</p> <p>First, learning is localized. The model only needs to answer a comparatively easier question: given a noisy sample, in which direction should we move to make it slightly less noisy? Second, the target distributions become smoother at high noise levels, which stabilizes training. Third, the framework naturally supports conditional control because reverse dynamics can be perturbed by gradients or conditional denoisers.</p> <p>At a high level, diffusion models solve generation by learning many small inverse steps rather than one globally difficult transformation.</p> <h2 id="score-functions-and-langevin-dynamics">Score functions and Langevin dynamics</h2> <p>The cleanest theoretical entry point is the <strong>score function</strong></p> \[\nabla_x \log p(x),\] <p>which tells us in which direction probability density increases most rapidly.</p> <p>If we knew the score exactly, then we could sample from the target distribution using <strong>Langevin dynamics</strong>. This score-based route to generative modeling was made practical by <a href="https://arxiv.org/abs/1907.05600">Song and Ermon (2019)</a>. In its overdamped form, the update is</p> \[x_{k+1} = x_k + \frac{\epsilon}{2}\nabla_x \log p(x_k) + \sqrt{\epsilon}\, z_k, \qquad z_k \sim \mathcal{N}(0, I).\] <p>The first term drifts the sample toward regions of high probability and the second term injects Gaussian noise so that the chain explores rather than collapses. Under suitable regularity conditions and sufficiently small step size, this Markov chain converges to the target density.</p> <p>In practice, we do not know $\nabla_x \log p(x)$, so we learn a neural approximation $s_\theta(x)$. This immediately gives the score-based sampling rule</p> \[x_{k+1} = x_k + \frac{\epsilon}{2} s_\theta(x_k) + \sqrt{\epsilon}\, z_k.\] <p>The difficulty is that the score of the raw data distribution can be hard to estimate accurately, especially when the data lie near a low-dimensional manifold inside a high-dimensional ambient space. This motivates a central diffusion idea: estimate scores not just for the original distribution, but for a family of progressively smoothed distributions.</p> <h2 id="noise-perturbation-and-annealed-score-estimation">Noise perturbation and annealed score estimation</h2> <p>Let $p_\sigma(x)$ denote the data distribution convolved with Gaussian noise of scale $\sigma$. For large $\sigma$, the distribution is smooth and easy to model; for small $\sigma$, it becomes sharp and data-like. Noise-conditioned score networks learn</p> \[s_\theta(x,\sigma) \approx \nabla_x \log p_\sigma(x)\] <p>for many noise scales $\sigma_1 &gt; \sigma_2 &gt; \dots &gt; \sigma_L$.</p> <p>Sampling then proceeds through <strong>annealed Langevin dynamics</strong>:</p> <ol> <li>Start from strong noise.</li> <li>Sample at a high noise level using the corresponding score estimate.</li> <li>Gradually reduce the noise level while continuing to refine the sample.</li> </ol> <p>This already contains the qualitative logic of diffusion: generation is a trajectory through a family of noise scales. DDPM later made this intuition particularly practical by choosing a simple forward corruption process and a tractable training objective.</p> <h2 id="forward-diffusion-in-ddpm">Forward diffusion in DDPM</h2> <p>The standard <strong>Denoising Diffusion Probabilistic Model (DDPM)</strong>, introduced by <a href="https://arxiv.org/abs/2006.11239">Ho, Jain, and Abbeel (2020)</a>, defines a forward noising chain</p> \[q(x_t \mid x_{t-1}) = \mathcal{N}\!\left(x_t; \sqrt{1-\beta_t}\, x_{t-1}, \beta_t I\right), \qquad t = 1,\dots,T,\] <p>where $\beta_t \in (0,1)$ is a variance schedule.</p> <p>Intuitively, each step keeps most of the previous signal but injects a small amount of Gaussian noise. After many steps, the original data sample $x_0$ is destroyed and $x_T$ becomes approximately standard Gaussian.</p> <p>Defining $\alpha_t = 1-\beta_t$ and $\bar{\alpha}<em>t = \prod</em>{i=1}^t \alpha_i$, one obtains the extremely useful closed form</p> \[x_t = \sqrt{\bar{\alpha}_t}\, x_0 + \sqrt{1-\bar{\alpha}_t}\,\epsilon, \qquad \epsilon \sim \mathcal{N}(0,I).\] <p>Equivalently,</p> \[q(x_t \mid x_0) = \mathcal{N}\!\left(x_t; \sqrt{\bar{\alpha}_t}\,x_0, (1-\bar{\alpha}_t)I\right).\] <p>This formula matters because it lets us sample $x_t$ from $x_0$ directly, without simulating the entire chain from step $1$ to $t$. During training, one may therefore choose a random time index $t$, synthesize the corresponding noisy sample in one step, and optimize a denoising objective.</p> <p><strong>The key asymmetry is worth keeping in view.</strong> The forward chain is deliberately simple and completely specified; the reverse chain is the learning problem. This is why DDPM can use a variational objective without ever needing the intractable marginal likelihood in closed form.</p> <ul> <li><strong>Forward process.</strong> <em>Known corruption.</em> Starting from $x_0$, the fixed Markov chain $q(x_t \mid x_{t-1})$ gradually removes information until $x_T$ is close to Gaussian noise. The schedule is a modeling choice, but no neural network is needed to evaluate or sample this chain.</li> <li><strong>Reverse process.</strong> <em>Learned denoising.</em> The true conditional $q(x_{t-1} \mid x_t)$ depends on the unknown data distribution. DDPM replaces it with $p_\theta(x_{t-1} \mid x_t)$, whose mean is predicted from a noisy input and a timestep embedding.</li> <li><strong>Training signal.</strong> <em>A local surrogate for a global density model.</em> Because $q(x_t \mid x_0)$ is analytically tractable, training can draw one random time, create one noisy observation, and ask the network to recover its noise or clean signal.</li> </ul> <figure style="margin: 1.75rem auto; max-width: 900px;"> <img src="/assets/img/blog/ddpm-forward-reverse-chain.png" alt="DDPM forward and reverse diffusion chains" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">The fixed forward chain and learned reverse chain in DDPM. Adapted from <a href="https://arxiv.org/abs/2006.11239">Ho, Jain, and Abbeel (2020)</a> with explanatory annotations from <a href="https://lilianweng.github.io/posts/2021-07-11-diffusion-models/">Weng (2021)</a>.</figcaption> </figure> <h2 id="reverse-diffusion-and-why-it-is-learnable">Reverse diffusion and why it is learnable</h2> <p>If we could sample from the true reverse conditionals $q(x_{t-1}\mid x_t)$, then we could start from $x_T \sim \mathcal{N}(0,I)$ and reconstruct data by repeatedly removing noise. The core modeling problem is therefore the reverse transition</p> \[p_\theta(x_{t-1}\mid x_t).\] <p>For sufficiently small $\beta_t$, the reverse process is approximately Gaussian. More specifically, conditioning on the original clean sample gives a tractable posterior:</p> \[q(x_{t-1} \mid x_t, x_0) = \mathcal{N}\!\left(x_{t-1}; \tilde{\mu}_t(x_t, x_0), \tilde{\beta}_t I\right),\] <p>with</p> \[\tilde{\beta}_t = \frac{1-\bar{\alpha}_{t-1}}{1-\bar{\alpha}_t}\beta_t\] <p>and</p> \[\tilde{\mu}_t(x_t,x_0) = \frac{\sqrt{\alpha_t}(1-\bar{\alpha}_{t-1})}{1-\bar{\alpha}_t}x_t + \frac{\sqrt{\bar{\alpha}_{t-1}}\beta_t}{1-\bar{\alpha}_t}x_0.\] <p>This expression shows why denoising is enough. If we can estimate either the clean sample $x_0$, the added noise $\epsilon$, or an equivalent score quantity from $x_t$, then we can parameterize the reverse mean and sample backward.</p> <h2 id="training-objectives-noise-score-and-variational-bounds">Training objectives: noise, score, and variational bounds</h2> <p>The original DDPM paper derives a variational lower bound on the data likelihood, but in practice the most widely used objective is the simplified noise-prediction loss</p> \[\mathcal{L}_{\text{simple}} = \mathbb{E}_{x_0,\epsilon,t} \left[ \left\|\epsilon - \epsilon_\theta(x_t,t)\right\|_2^2 \right].\] <p>This objective can look deceptively simple. Its effectiveness comes from a structural fact: predicting the Gaussian perturbation is equivalent to learning how to denoise samples across many signal-to-noise regimes.</p> <p>There are several equivalent parameterizations.</p> <ul> <li><strong>Noise prediction</strong> predicts $\epsilon$ directly.</li> <li><strong>Clean-data prediction</strong> predicts $x_0$.</li> <li><strong>Score prediction</strong> predicts a scaled score field.</li> <li><strong>Velocity prediction</strong> uses a linear combination of $x_0$ and $\epsilon$, which is now common in modern latent diffusion and video systems.</li> </ul> <p>These parameterizations are not identical numerically even if they are closely related analytically. They change gradient scaling across timesteps, interact differently with solver design, and can meaningfully affect optimization stability.</p> <figure style="margin: 1.75rem auto; max-width: 760px;"> <img src="/assets/img/blog/ddpm-progressive-decoding.jpg" alt="DDPM progressive generation on CIFAR-10" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">Progressive generation in DDPM: large-scale image structure appears before fine details. Reproduced from <a href="https://arxiv.org/abs/2006.11239">Ho, Jain, and Abbeel (2020)</a>.</figcaption> </figure> <h2 id="model-architecture">Model architecture</h2> <p>The diffusion objective specifies <em>what</em> the network should predict, but the backbone determines <em>which spatial and semantic information</em> it can combine at each noise level. The two dominant choices are <strong>U-Net</strong> and <strong>Transformer</strong> backbones.</p> <p><strong>U-Net.</strong> The original <a href="https://arxiv.org/abs/1505.04597">U-Net</a> has a downsampling stack, an upsampling stack, and skip connections between matching resolutions. In diffusion, the down path aggregates global context while the up path restores local detail; skip connections preserve spatial information that would otherwise be lost. Timestep embeddings, residual blocks, and attention layers are added to tell the network how much noise remains and which condition it should follow.</p> <ul> <li><strong>Downsampling.</strong> <em>Build global context.</em> Repeated convolution or residual blocks reduce resolution and increase channel capacity, allowing the network to reason about object identity and long-range structure.</li> <li><strong>Upsampling.</strong> <em>Recover local detail.</em> Features are expanded back to the target resolution, while skip connections inject high-frequency information from the corresponding encoder stage.</li> </ul> <figure style="margin: 1.75rem auto; max-width: 900px;"> <img src="/assets/img/blog/unet-architecture.png" alt="Original U-Net architecture" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">The classic U-Net encoder-decoder with skip connections. Reproduced from <a href="https://arxiv.org/abs/1505.04597">Ronneberger, Fischer, and Brox (2015)</a>.</figcaption> </figure> <p><strong>Diffusion Transformers (DiT).</strong> <a href="https://arxiv.org/abs/2212.09748">DiT</a> replaces convolutional hierarchy with tokenized latent patches and Transformer blocks. This is attractive when scaling model size and attention capacity matters more than a fixed convolutional inductive bias. The key tradeoff is that attention can model broad interactions naturally, while the latent-space encoder and decoder must shoulder much of the local image structure.</p> <figure style="margin: 1.75rem auto; max-width: 680px;"> <img src="/assets/img/blog/dit-architecture.png" alt="Diffusion Transformer architecture" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">A Diffusion Transformer block conditioned on timestep and class information. Reproduced from <a href="https://arxiv.org/abs/2212.09748">Peebles and Xie (2023)</a>.</figcaption> </figure> <h2 id="the-role-of-the-noise-schedule">The role of the noise schedule</h2> <p>The schedule ${\beta_t}$ or, equivalently, the log signal-to-noise ratio profile, is not a detail. It determines which corruption levels the model sees and how probability mass moves across time.</p> <p>If early timesteps destroy the signal too quickly, the reverse process becomes unnecessarily hard. If the schedule is too conservative, training may waste capacity on nearly trivial denoising steps. This is why later work increasingly reasons in terms of continuous-time noise schedules, log-SNR parameterizations, or solver-friendly path design.</p> <p>From a practical standpoint, one of the most important lessons is that diffusion performance depends not just on model architecture, but on the joint design of:</p> <ul> <li>noise schedule,</li> <li>target parameterization,</li> <li>timestep sampling strategy,</li> <li>and sampling solver.</li> </ul> <h2 id="connection-to-score-based-sdes">Connection to score-based SDEs</h2> <p>The discrete DDPM chain and the score-based modeling line can be unified in the continuous-time SDE framework of <a href="https://arxiv.org/abs/2011.13456">Song et al. (2021)</a>. Instead of indexing the state by discrete steps $t=1,\dots,T$, we consider a continuous process</p> \[d x = f(x,t)\,dt + g(t)\,d w_t,\] <p>where $w_t$ is standard Brownian motion.</p> <p>The reverse-time dynamics then depend on the score:</p> \[d x = \left[f(x,t) - g(t)^2 \nabla_x \log p_t(x)\right]dt + g(t)\,d \bar{w}_t.\] <p>This formulation unifies several models:</p> <ul> <li>variance-preserving SDEs recover DDPM-like behavior,</li> <li>variance-exploding SDEs resemble annealed score models,</li> <li>probability-flow ODEs replace stochastic reverse-time evolution with deterministic trajectories sharing the same marginals.</li> </ul> <p>The SDE view is conceptually valuable because it decouples training from a specific discrete sampler and clarifies why better numerical solvers can improve generation without retraining the denoiser.</p> <h2 id="ddim-and-the-separation-of-training-from-sampling">DDIM and the separation of training from sampling</h2> <p>One of the most important developments after DDPM was <strong>Denoising Diffusion Implicit Models (DDIM)</strong> by <a href="https://arxiv.org/abs/2010.02502">Song, Meng, and Ermon (2021)</a>. The key insight is that the DDPM objective does not force us to sample with the original ancestral Markov chain. We can define a non-Markovian family of reverse processes that preserve the same training objective but admit deterministic trajectories.</p> <p>In practice, DDIM uses the denoiser to estimate $x_0$ and then updates the sample using a chosen timestep subsequence. In its deterministic form, the update can be interpreted as moving along a probability-flow-like path instead of repeatedly injecting fresh noise.</p> <p>This had two major effects on the field.</p> <p>First, it showed that sampling speed can be improved dramatically without retraining. Second, it made clear that <strong>the diffusion model is a learned time-dependent vector field or denoiser, while the sampler is a separate numerical design choice</strong>. Much of the later progress on fast diffusion sampling follows directly from this separation.</p> <h2 id="guided-diffusion-for-conditional-generation">Guided diffusion for conditional generation</h2> <p>Diffusion became far more useful once the reverse trajectory could be steered toward desired semantics. Guidance methods provide this control.</p> <h3 id="classifier-guidance">Classifier guidance</h3> <p>Suppose we want to sample conditionally on label or text information $y$. By Bayes’ rule,</p> \[\nabla_{x_t}\log p(x_t \mid y) = \nabla_{x_t}\log p(x_t) + \nabla_{x_t}\log p(y \mid x_t).\] <p>The second term can be estimated by a noisy classifier, as in <a href="https://arxiv.org/abs/2105.05233">Dhariwal and Nichol (2021)</a>. During sampling, one adds the classifier gradient to the reverse update, often with a scale factor $w$ controlling the strength of conditioning.</p> <p>This increases fidelity to the condition, but it also introduces a tradeoff: strong guidance typically improves alignment and sharpness while reducing diversity and sometimes causing oversaturation or other artifacts.</p> <h3 id="classifier-free-guidance">Classifier-free guidance</h3> <p>Classifier guidance requires an external classifier. <strong>Classifier-free guidance (CFG)</strong> from <a href="https://arxiv.org/abs/2207.12598">Ho and Salimans (2022)</a> avoids that by training the denoiser with conditioning dropped at random. At inference time, one evaluates both conditional and unconditional predictions and combines them:</p> \[s_{\text{guided}} = s_{\text{uncond}} + w\left(s_{\text{cond}} - s_{\text{uncond}}\right).\] <p>This is one of the most influential practical ideas in diffusion modeling. It is simple, effective, and easy to integrate with text, layout, or other side information. Most modern image and video diffusion systems rely on some form of CFG or a closely related variant.</p> <figure style="margin: 1.75rem auto; max-width: 760px;"> <img src="/assets/img/blog/score-sde-conditional-generation.jpg" alt="Conditional image generation with score-based SDEs" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">Conditional score-based generation for inpainting and colorization. Reproduced from <a href="https://arxiv.org/abs/2011.13456">Song et al. (2021)</a>.</figcaption> </figure> <h2 id="discrete-diffusion-models">Discrete diffusion models</h2> <p>Gaussian noise is natural for images, audio waveforms, and continuous latent spaces, but many important domains are inherently discrete: text tokens, molecular symbols, amino-acid sequences, graph labels, and other categorical states. In such settings, continuous perturbation followed by rounding is often statistically awkward.</p> <p>This motivates <strong>discrete diffusion</strong>.</p> <p>Instead of adding Gaussian noise, we define a Markov chain over categorical variables. For a one-hot representation, a forward step can be written as</p> \[q(x_t \mid x_{t-1}) = \mathrm{Cat}(Q_t x_{t-1}),\] <p>where $Q_t$ is a transition matrix.</p> <p>Different choices of $Q_t$ produce different notions of corruption:</p> <ul> <li>uniform random replacement,</li> <li>masking into a special absorbing state,</li> <li>nearest-neighbor or structure-aware transitions,</li> <li>task-specific kernels informed by chemistry, syntax, or graph topology.</li> </ul> <p>The design space is large because discrete corruption has no canonical Gaussian analogue. This is both a challenge and an opportunity. Good transition kernels can encode prior structure that would be difficult to express in a purely continuous formulation.</p> <h3 id="d3pm-and-structured-transition-kernels">D3PM and structured transition kernels</h3> <p>The <strong>D3PM</strong> family from <a href="https://arxiv.org/abs/2107.03006">Austin et al. (2021)</a> generalized multinomial diffusion by allowing structured discrete transitions rather than only uniform corruption. That flexibility made it easier to adapt diffusion ideas to domains where token relationships matter.</p> <p>From a modeling standpoint, D3PM is important because it emphasizes that the forward corruption kernel is part of the inductive bias. For language, molecules, or biological sequences, not every token substitution should be treated as equally plausible.</p> <figure style="margin: 1.75rem auto; max-width: 900px;"> <img src="/assets/img/blog/d3pm-discrete-process.png" alt="D3PM discrete forward and reverse processes" style="display: block; width: 100%; height: auto;" loading="lazy"/> <figcaption style="margin-top: 0.65rem; text-align: center; font-size: 0.9rem;">Different discrete transition kernels induce meaningfully different corruption paths and reverse problems. Reproduced from <a href="https://arxiv.org/abs/2107.03006">Austin et al. (2021)</a>.</figcaption> </figure> <h3 id="ratio-based-and-entropy-based-discrete-methods">Ratio-based and entropy-based discrete methods</h3> <p>Recent work such as <strong>SEDD</strong> by <a href="https://arxiv.org/abs/2310.16834">Lou, Meng, and Ermon (2024)</a> revisits how to parameterize the reverse process in discrete spaces. Instead of forcing the model into an image-style denoising template, these methods derive objectives more directly aligned with discrete probability ratios or entropy-based characterizations.</p> <p>This line of work suggests an important broader lesson: diffusion should be understood as a framework for learning reverse stochastic transport, not merely as “Gaussian noise plus a U-Net.”</p> <h2 id="a-unifying-view-across-variants">A unifying view across variants</h2> <p>Despite the diversity of formulations, the core structure of diffusion models is remarkably consistent:</p> <ol> <li>Choose a tractable corruption or interpolation process.</li> <li>Learn a local reverse operator, usually a denoiser or score field.</li> <li>Integrate those local predictions into a global sampling trajectory.</li> </ol> <p>The main axes of variation are:</p> <ul> <li><strong>state space</strong>: continuous or discrete,</li> <li><strong>time</strong>: discrete chain or continuous SDE/ODE,</li> <li><strong>prediction target</strong>: noise, score, clean sample, velocity, or probability ratio,</li> <li><strong>conditioning</strong>: unconditional, classifier-guided, classifier-free guided, or multimodal,</li> <li><strong>sampler</strong>: ancestral, implicit, deterministic, solver-based, or distilled.</li> </ul> <p>Once viewed this way, many “new” diffusion papers become easier to parse. They usually innovate along one or two of these axes rather than reinventing the whole framework.</p> <h2 id="what-has-mattered-most-historically">What has mattered most historically</h2> <p>If I compress the historical arc into a few central ideas, these are the ones I would keep.</p> <p><strong>Denoising became a scalable route to generative modeling.</strong> DDPM showed that high-quality generation can emerge from repeated local denoising even when every latent has the same dimensionality as the data. This avoids a brittle global inversion problem: the network learns local corrections across a controlled range of signal-to-noise ratios, while the Markov structure supplies the global composition rule.</p> <p><strong>Score estimation provided the theoretical backbone.</strong> The score-based perspective explains why a denoiser contains usable geometric information about the data distribution and why reverse stochastic dynamics are principled rather than heuristic. It also tells us what can be changed safely: a new sampler or noise path should remain compatible with the score field that the training objective estimates.</p> <p><strong>Sampling is a numerical-analysis problem as much as a modeling problem.</strong> DDIM, higher-order solvers, consistency methods, and distillation all reinforce the same point: once the reverse field is learned, quality and efficiency depend strongly on how we integrate that field. In practice, one should separate failures caused by a weak denoiser from failures caused by too few steps, an unsuitable noise schedule, or an unstable solver.</p> <p><strong>Discrete structure is becoming increasingly important.</strong> As diffusion expands from images to scientific design, biology, language, and multimodal reasoning, respecting the native state space becomes essential. A transition kernel that respects masking, chemistry, syntax, or graph structure can be more valuable than simply importing a continuous-image recipe and rounding its output.</p> <h2 id="outlook">Outlook</h2> <p>Diffusion models are no longer just a method for image synthesis. They now form a broader language for generative modeling that connects stochastic processes, conditional control, and probability transport. For research problems involving structured uncertainty, iterative refinement, or native discrete constraints, that language is especially useful.</p> <p>The practical frontier is still moving quickly: better samplers, better parameterizations, latent-space diffusion, consistency-style acceleration, diffusion transformers, and task-specific discrete-state models are all active directions. But the central mathematical intuition has remained stable: learn how to reverse a tractable corruption process, and generation becomes the accumulation of many small, well-behaved local steps.</p> <h2 id="references">References</h2> <ol> <li>Jascha Sohl-Dickstein, Eric Weiss, Niru Maheswaranathan, and Surya Ganguli. <em>Deep Unsupervised Learning using Nonequilibrium Thermodynamics</em>. ICML 2015. https://arxiv.org/abs/1503.03585</li> <li>Yang Song and Stefano Ermon. <em>Generative Modeling by Estimating Gradients of the Data Distribution</em>. NeurIPS 2019. https://arxiv.org/abs/1907.05600</li> <li>Jonathan Ho, Ajay Jain, and Pieter Abbeel. <em>Denoising Diffusion Probabilistic Models</em>. NeurIPS 2020. https://arxiv.org/abs/2006.11239</li> <li>Jiaming Song, Chenlin Meng, and Stefano Ermon. <em>Denoising Diffusion Implicit Models</em>. ICLR 2021. https://arxiv.org/abs/2010.02502</li> <li>Yang Song, Jascha Sohl-Dickstein, Diederik P. Kingma, Abhishek Kumar, Stefano Ermon, and Ben Poole. <em>Score-Based Generative Modeling through Stochastic Differential Equations</em>. ICLR 2021. https://arxiv.org/abs/2011.13456</li> <li>Prafulla Dhariwal and Alexander Nichol. <em>Diffusion Models Beat GANs on Image Synthesis</em>. NeurIPS 2021. https://arxiv.org/abs/2105.05233</li> <li>Jonathan Ho and Tim Salimans. <em>Classifier-Free Diffusion Guidance</em>. 2022. https://arxiv.org/abs/2207.12598</li> <li>Jacob Austin, Daniel D. Johnson, Jonathan Ho, Daniel Tarlow, and Rianne van den Berg. <em>Structured Denoising Diffusion Models in Discrete State-Spaces</em>. NeurIPS 2021. https://arxiv.org/abs/2107.03006</li> <li>Tiankai Hang, Shuyang Gu, Chen Li, Jianmin Bao, Dong Chen, Han Hu, Xin Geng, Baining Guo, and Daxin Jiang. <em>Efficient Diffusion Training via Min-SNR Weighting Strategy</em>. ICCV 2023. https://arxiv.org/abs/2303.09556</li> <li>Aaron Lou, Chenlin Meng, and Stefano Ermon. <em>Discrete Diffusion Modeling by Estimating the Ratios of the Data Distribution</em>. ICML 2024. https://arxiv.org/abs/2310.16834</li> <li>Olaf Ronneberger, Philipp Fischer, and Thomas Brox. <em>U-Net: Convolutional Networks for Biomedical Image Segmentation</em>. MICCAI 2015. https://arxiv.org/abs/1505.04597</li> <li>William Peebles and Saining Xie. <em>Scalable Diffusion Models with Transformers</em>. ICCV 2023. https://arxiv.org/abs/2212.09748</li> </ol>]]></content><author><name></name></author><category term="research"/><category term="diffusion-models"/><category term="generative-ai"/><category term="score-based-modeling"/><summary type="html"><![CDATA[A technical overview of score-based generative modeling, DDPM, DDIM, guided diffusion, and discrete diffusion models.]]></summary></entry></feed>